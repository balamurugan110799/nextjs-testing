/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
    domains: ['https://balamurugan110799.github.io/nextjs-testing'],
    unoptimized: true,
    },
};

export default nextConfig;
